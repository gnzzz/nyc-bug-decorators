{
  "name": "nyc-bug-decorators",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.ts",
  "scripts": {
    "start": "npm run build",
    "build": "nyc instrument src instrumented && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@istanbuljs/nyc-config-typescript": "^1.0.1",
    "nyc": "^15.1.0",
    "typescript": "^4.2.2"
  }
}
